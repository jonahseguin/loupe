{
  "name": "loupe",
  "displayName": "Loupe",
  "description": "Review changes inline in your real files. Comment in the margin, hand notes to Claude.",
  "version": "0.0.1",
  "publisher": "internetbackyard",
  "engines": { "vscode": "^1.85.0" },
  "main": "./dist/extension.js",
  "activationEvents": ["onStartupFinished"],
  "contributes": {
    "commands": [
      { "command": "loupe.toggle", "title": "Toggle Review Mode (diff)", "category": "Loupe" },
      { "command": "loupe.copyForClaude", "title": "Copy Comments", "category": "Loupe", "icon": "$(clippy)" },
      { "command": "loupe.createComment", "title": "Add Comment", "category": "Loupe" },
      { "command": "loupe.deleteComment", "title": "Delete Comment", "category": "Loupe", "icon": "$(trash)" },
      { "command": "loupe.addCommentHere", "title": "Add Comment", "category": "Loupe", "icon": "$(comment-add)" },
      { "command": "loupe.clearComments", "title": "Clear All Comments", "category": "Loupe", "icon": "$(clear-all)" }
    ],
    "keybindings": [
      { "command": "loupe.copyForClaude", "key": "ctrl+alt+c", "mac": "cmd+alt+c", "when": "editorTextFocus" },
      { "command": "loupe.addCommentHere", "key": "ctrl+alt+m", "mac": "cmd+alt+m", "when": "editorTextFocus" },
      { "command": "loupe.toggle", "key": "ctrl+alt+r", "mac": "cmd+alt+r" }
    ],
    "menus": {
      "commandPalette": [
        { "command": "loupe.createComment", "when": "false" },
        { "command": "loupe.deleteComment", "when": "false" }
      ],
      "editor/context": [
        { "command": "loupe.addCommentHere", "when": "editorTextFocus", "group": "1_modification@10" }
      ],
      "comments/commentThread/context": [
        { "command": "loupe.createComment", "group": "inline", "when": "commentController == loupe" }
      ],
      "comments/comment/title": [
        { "command": "loupe.deleteComment", "group": "inline", "when": "commentController == loupe" }
      ],
      "view/title": [
        { "command": "loupe.copyForClaude", "when": "view == loupe.changes", "group": "navigation@1" },
        { "command": "loupe.clearComments", "when": "view == loupe.changes", "group": "navigation@2" }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        { "id": "loupe", "title": "Loupe", "icon": "resources/loupe.svg" }
      ]
    },
    "views": {
      "loupe": [
        { "id": "loupe.changes", "name": "Comments & Changes" }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "bundle": "node esbuild.js",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run bundle",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/test-cli": "^0.0.6",
    "@vscode/test-electron": "^2.3.9",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
